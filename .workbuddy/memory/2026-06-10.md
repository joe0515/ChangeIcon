# 2026-06-10 工作日志

## 项目构建
- 读取项目关键文件 (Package.swift, 源码, 构建脚本)
- 构建脚本 `build_app.sh` 因 SwiftPM 沙箱限制失败 → 改用 `swift build -c release --disable-sandbox` 成功
- 手动完成 helper 编译 (`seticon_helper.swift` → `build/seticon`)
- 手动完成 .app 打包 (Info.plist, 资源文件, 二进制, 图标库)
- Ad-hoc 签名完成
- 构建产物: `build/ChangeIcon.app` (18MB, arm64, macOS 14+)
- 更新 CHANGELOG.md 中「构建」待验证项为已完成
- DMG 打包: `build/ChangeIcon-0.5.2-arm64.dmg` (16 MB, UDZO 压缩)
- 最终方案：放弃 App Management 自动检测。评估结论 — macOS 无公开 API 检测 kTCCServiceSystemPolicyAppBundles；TCC.db 需 FDA 但 ad-hoc 签名下 client 字段不可预测；Apple Events API 检测的是 Automation 而非 App Management。改为纯手动步骤：PermissionGuideView 显示「去开启」按钮+说明文字，SettingsView 显示蓝色「请手动确认」，不再标记为"未开启"。
